#pragma once
#ifndef OTOS_TAGS_H
#define OTOS_TAGS_H

namespace cfp::tags {

  /*!
   * @struct ratio
   * @brief a tag to use in some generic context
   */
  struct ratio  {};

  /*!
   * @struct lagged
   * @brief a tag to use in some generic context
   */
  struct lagged {};

  /*!
   * @struct online
   * @brief a tag to use in some generic context
   */
  struct online {};

  /*!
   * @struct covariance
   * @brief a tag to use in some generic context
   */
  struct covariance {};

  /*!
   * @struct variance
   * @brief a tag to use in some generic context
   */
  struct variance {};

}

#endif
